{
  "name": "boxGetToken",
  "version": "1.0.0",
  "description": "A project to refresh Box OAuth token",
  "main": "api/refresh-token.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "mongodb": "^4.1.0",
    "querystring": "^0.2.1"
  },
  "engines": {
    "node": "16.x"
  }
}
